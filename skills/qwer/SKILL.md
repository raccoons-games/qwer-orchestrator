---
name: qwer
description: Start, clone, resume, or set up development work on a RaccoonsGames project — the explicit trigger for the orchestrator playbook. Use ONLY when the user runs the literal command /qwer (with an optional project name/description as argument). Do not self-trigger this from natural-language phrasing like "let's work on X" — that must go through this command.
---

Read `~/.claude/orchestrator/ORCHESTRATOR.md` and follow it, using any argument passed to this command (project name, description, or vague reference) to resolve the target repo in Step 1. If no argument was given, ask the user which project they mean before proceeding.
