---
purpose: cross-project accumulated lessons for the frontend-dev role
audience: Claude, any RaccoonsGames project generating or running a frontend-dev subagent
---

# frontend-dev — accumulated lessons

No accumulated lessons yet. This role is for Node/TS client-side work: pages/components, UI state,
client-side data fetching against an existing API. Contribute short, timestamped, generic entries here
as you learn things worth carrying forward to the next project — no project names, ticket IDs, or design
specifics (see the subagent template's Tier B section for why); project-specific trivia belongs in the
repo's own CLAUDE.md.
